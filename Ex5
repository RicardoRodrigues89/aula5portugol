programa
{
//Escreva um pseudocódigo em Portugol que receba números do usuário até que
//ele digite 0 e, em seguida, imprime a soma de todos os números digitados.

	funcao inicio()
	{
		inteiro numero, soma
		soma = 0

   faca
   {
      escreva("Digite um número (ou 0 para encerrar): ")
      leia(numero)

      soma = soma + numero
   } enquanto (numero != 0)

   escreva("A soma dos números digitados é: ", soma)
	}
}